import { defineConfig } from 'vite';

// IMPORTANT: if your GitHub repo name is not "nsw-sa2-geojson",
// change the base path below to match the repo name: `/<your-repo-name>/`
export default defineConfig({
  base: '/nsw-sa2-geojson/',
});